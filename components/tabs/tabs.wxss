.tabs{}
.tabs_title{
    display: flex;
}
.title_item{
    flex: 1;
    display: flex;
    padding: 10rpx 0;
    justify-content: center;
    align-items: center;
}
.active{
    color:var(--themeColor);
    border-bottom: 5rpx solid currentColor;
}
.tabs_content{}